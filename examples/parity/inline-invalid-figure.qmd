---
title: "uv-python invalid inline figure"
engine: uv-python
format: html
execute:
  echo: false
---

```{python}
import matplotlib.pyplot as plt

fig, ax = plt.subplots()
ax.plot([1, 2, 3], [1, 4, 9])
```

Inline figure output must fail: `{python} fig`.
